import React from 'react';
import connect from '../../assets/images/bg.png'

const Connect = () => {
    return (
        <section style={{background:`url(${connect})`}}>
            
        </section>
    );
};

export default Connect;